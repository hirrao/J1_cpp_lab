aux_source_directory(. DLL)
add_library(DLL SHARED ${DLL})
SET_TARGET_PROPERTIES(DLL PROPERTIES LINKER_LANGUAGE CXX
        RUNTIME_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/bin
        LIBRARY_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/lib
        OUTPUT_NAME "DLL"
        PREFIX ""
        SUFFIX ".dll")